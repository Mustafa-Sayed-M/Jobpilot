function CandidatesList({ className = '', candidatesList = [] }) {
    return (
        <div className={`candidates-list ${className}`}>
            Candidates List
        </div>
    )
}

export default CandidatesList;